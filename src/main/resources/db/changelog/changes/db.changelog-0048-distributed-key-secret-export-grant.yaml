databaseChangeLog:
  - changeSet:
      id: 0048-distributed-key-secret-export-grant
      author: codex
      preConditions:
        - onFail: MARK_RAN
        - not:
            - tableExists:
                tableName: distributed_key_secret_export_grant
      changes:
        - createTable:
            tableName: distributed_key_secret_export_grant
            columns:
              - column: { name: id, type: BIGINT, autoIncrement: true, constraints: { primaryKey: true, nullable: false } }
              - column: { name: distributed_key_id, type: BIGINT, constraints: { nullable: false } }
              - column: { name: token_hash, type: VARCHAR(128), constraints: { nullable: false } }
              - column: { name: full_key_ciphertext, type: TEXT, constraints: { nullable: false } }
              - column: { name: source_action, type: VARCHAR(32), constraints: { nullable: false } }
              - column: { name: expires_at, type: TIMESTAMP WITH TIME ZONE, constraints: { nullable: false } }
              - column: { name: consumed_at, type: TIMESTAMP WITH TIME ZONE }
              - column: { name: revoked_at, type: TIMESTAMP WITH TIME ZONE }
              - column: { name: created_at, type: TIMESTAMP WITH TIME ZONE, defaultValueComputed: CURRENT_TIMESTAMP, constraints: { nullable: false } }
              - column: { name: updated_at, type: TIMESTAMP WITH TIME ZONE, defaultValueComputed: CURRENT_TIMESTAMP, constraints: { nullable: false } }
        - addForeignKeyConstraint:
            baseTableName: distributed_key_secret_export_grant
            baseColumnNames: distributed_key_id
            referencedTableName: distributed_key
            referencedColumnNames: id
            constraintName: fk_key_secret_export_distributed_key
            onDelete: CASCADE
        - addUniqueConstraint:
            tableName: distributed_key_secret_export_grant
            columnNames: token_hash
            constraintName: uk_key_secret_export_token_hash
        - createIndex:
            tableName: distributed_key_secret_export_grant
            indexName: idx_key_secret_export_key
            columns:
              - column: { name: distributed_key_id }
        - createIndex:
            tableName: distributed_key_secret_export_grant
            indexName: idx_key_secret_export_expires_at
            columns:
              - column: { name: expires_at }
        - createIndex:
            tableName: distributed_key_secret_export_grant
            indexName: idx_key_secret_export_consumed_at
            columns:
              - column: { name: consumed_at }
        - createIndex:
            tableName: distributed_key_secret_export_grant
            indexName: idx_key_secret_export_revoked_at
            columns:
              - column: { name: revoked_at }
