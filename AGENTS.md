Always respond in Chinese (Simplified).
- 所有回复、文档、注释、代码解释都使用简体中文。
- 专有名词（如 API、变量名、函数名）保留英文。
- 使用 Markdown 格式，结构清晰。

Workflow requirements
- 默认使用本地优先协作流程，不再强制使用线上 Notion 或线上 Linear。
- 只有用户明确要求使用线上 Notion、Linear 或其他 SaaS 工具时，才调用对应线上连接器。
- 为提高工作效率，在任务边界清晰、写入范围可划分、结果可独立验证时，可以将任务合理分发给 subagents 并行处理；主 agent 负责总体规划、上下文同步、结果集成与最终验收。
- 已完成、已过时或不再需要的 subagents 必须及时清理回收，避免上下文漂移、重复工作和资源占用。
- 推荐目录：需求文档放在 `docs/requirements/`，分析报告放在 `docs/reports/`，决策记录放在 `docs/decisions/`，迁移记录放在 `docs/migrations/`，任务放在 `tasks/backlog/`、`tasks/in-progress/`、`tasks/done/`。
- 任何需求在开始编码前，必须先在当前项目仓库内创建或更新本地 Markdown 文档，记录需求背景、目标、方案、范围、风险和验收标准。
- 任何需求在开始编码前，必须先在当前项目仓库内创建或更新本地任务文件；任务必须与需求、报告、决策或迁移记录建立清晰关联，避免无归属、无追溯的任务。
- 任务体系必须默认采用父任务、子任务和 task spec：
  - 父任务用于承接一个明确的上游来源，例如需求文档、分析报告、决策记录或迁移记录，并定义整体目标、范围、非目标、风险、验收边界和测试边界。
  - 子任务用于把父任务拆分为可独立执行、独立交付、独立验证的工作单元；每个子任务必须归属于一个父任务，并说明自己的输出、边界、依赖和验证方式。
  - task spec 用于固定任务边界，至少包含：背景、目标、非目标、上游来源、输入、输出、影响范围、依赖、风险、验收标准、测试边界、关联文档、关联任务和当前状态。
- 不接受只有笼统动词的任务，例如“优化一下”“处理问题”“完善功能”“修复 bug”。遇到这类任务时，必须先补充 task spec 或拆分为边界清晰的父任务/子任务，再进入实现。
- 任务边界必须可确认、可划分：每个任务需要说明包含哪些文件、模块、流程或数据范围，也需要说明不包含哪些范围。
- 测试边界必须可确认、可划分：每个任务需要说明对应的验证方式、测试命令、手工检查点或不适用测试的原因；父任务记录整体测试策略，子任务记录各自的验证口径。
- 父任务和子任务的状态流转必须反映真实进度；已完成的任务移动或记录到 `tasks/done/`，未完成的增强项必须拆分回 `tasks/backlog/` 或保留在 `tasks/in-progress/`，不能只留在总结里。
- 在本地文档和本地任务完成前，不进入代码实现、代码修改或交付阶段；如果缺少必要上下文，优先补全需求文档、父任务、子任务和 task spec。
- 代码实现过程中，如方案、范围、风险、进度或验收结果发生变化，需要同步更新对应本地文档和本地任务。
- 代码交付完成后，必须及时回写本地文档与本地任务，至少补充实现结果、测试/验证情况、遗留问题、后续建议，并更新对应状态；如果父任务仍未完全闭环，需要明确剩余子任务和下一步验收边界。
