# X-38 关单收口（2026-04-13）

## 实现结果
- `GatewayUsage` 新增 `savedInputTokens()` / `isEmpty()`
- `GatewayObservabilityService.recordCacheUsage()` 已切到归一化后的 `savedInputTokens()`
- OpenAI / Anthropic / Gemini 流式链路统一到 `FINAL / PARTIAL` usage 完整度模型
- `ObservabilitySummaryResponse`、`AnalyticsOverviewResponse`、`DashboardOverviewResponse` 已扩展 `usage_record` 计数、`cacheSourceBreakdown`、`usageCompletenessBreakdown`

## 验证结果
- `./gradlew test`：通过
- `AnalyticsQueryServiceTests` / `ObservabilityQueryServiceTests`：通过
- `GatewayEndToEndSmokeTests`：通过

## 遗留问题
- `cacheSourceBreakdown` 当前仍基于 `cache_hit_log.cacheKind`

## 后续建议
- 可继续把 saved token 折算成估算成本收益，直接进入 dashboard
