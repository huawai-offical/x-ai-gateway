# X-30 关单收口（2026-04-13）

## 实现结果
- 新增迁移：`db.changelog-0016-request-usage-audit.yaml`
- 新增持久化结构：`request_log`、`usage_record`、`audit_log`
- 新增 `GatewayRequestLifecycleService` 统一写入请求与 usage
- 新增 `GatewayAuditLogService` + `AdminAuditWebFilter` 统一记录后台非 GET `/admin/**` 请求
- 新增 Micrometer 指标：`gateway.request.total`、`gateway.request.duration`、`gateway.usage.total_tokens`、`gateway.usage.saved_input_tokens`、`gateway.cache.hit_tokens`、`gateway.cache.write_tokens`

## 验证结果
- `./gradlew test`：通过
- `GatewayEndToEndSmokeTests`：通过
- 真实 PostgreSQL + Liquibase smoke：通过

## 遗留问题
- 默认 H2 全量回归仍对 `site_model_capability` 做测试夹具兜底；真实 PostgreSQL + Liquibase 已单独验证

## 后续建议
- 后续如要做更细粒度运营报表，可继续基于 `usage_record` 扩展聚合口径
