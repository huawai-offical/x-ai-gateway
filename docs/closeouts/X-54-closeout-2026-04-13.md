# X-54 关单收口（2026-04-13）

## 实现结果
- 新增 `gateway.core.response` 内部响应层：`GatewayResponse`、`GatewayStreamResponse`、`GatewayStreamEvent`、`GatewayUsageView`、`GatewayFinishReason`、`GatewayErrorView`
- 新增协议编码器：`OpenAiChatCompletionEncoder`、`OpenAiResponsesEncoder`、`AnthropicMessagesEncoder`、`GeminiGenerateContentEncoder`
- OpenAI / Anthropic / Gemini controller 已切到 encoder 分层
- 聊天 runtime 已补齐 `finishReason` 透传，OpenAI / Anthropic / Gemini 流式优先读取 provider metadata

## 验证结果
- `./gradlew test`：通过
- `GatewayProtocolEncoderContractTests`：通过
- 三套协议 controller 测试：通过

## 遗留问题
- `GatewayErrorView` 已建模，但协议级错误 payload 仍主要走现有异常处理链

## 后续建议
- 后续扩展新协议或新资源对象时，继续沿用 `GatewayResponse / GatewayStreamEvent -> encoder` 分层
