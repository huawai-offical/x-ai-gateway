# X-50 关单收口（2026-04-13）

## 实现结果
- 新增 affinity 可替换底座：`AffinityBindingStore`、`RedisAffinityBindingStore`
- `AffinityCacheService` 改为依赖可替换存储后端
- 新增 `GatewayEndToEndSmokeTests`
  - 真实 Spring 容器
  - H2 默认回归
  - 可替换的内存 affinity store
  - fake chat runtime
- 覆盖 prefix affinity 复用、失败失效、绑定失活后的 credential 切换、preview/explain 一致性与 admin 审计

## 验证结果
- `./gradlew test`：通过
- `GatewayEndToEndSmokeTests`：通过
- 真实 PostgreSQL + Liquibase smoke：通过

## 遗留问题
- 默认全量回归仍未纳入真实 Redis

## 后续建议
- 后续可以继续把网络治理、代理池、TLS 指纹扩进同一套集成夹具
